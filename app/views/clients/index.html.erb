<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <h1>Client list</h1>
 
      <table class="table table-striped">
        <tr>
          <th>Firstname</th>
          <th>Surname</th>
          <th>Phone #</th>
          <th># Dogs</th>
          <th></th>
        </tr>
       
        <% @clients.order(:surname).each do |client| %>
          <tr>
            <td><%= client.firstname %></td>
            <td><%= client.surname %></td>
            <td><%= client.phonenum_format %></td>
            <td><%= client.dogs.count %></td>
            <td><%= link_to 'Show', client_path(client), class: 'btn btn-default' %>
            <%= link_to 'Edit', edit_client_path(client), class: 'btn btn-warning' %>
            <%# link_to 'Destroy', client_path(client), class: 'btn btn-danger',
                    method: :delete,
                    data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </table>

      <%= link_to 'Back', :back, class: 'btn btn-primary' %>
      <%= link_to 'Home', root_path, class: 'btn btn-primary' %>
    </div>
  </div>
</div>
