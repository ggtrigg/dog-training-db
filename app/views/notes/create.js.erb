$("<%= escape_javascript(render @note) %>").appendTo("#notes-container");
$("#note-count").text("<%= @note.annotatable.notes.count %>");
$('.flash-wrapper').html("<%= escape_javascript(render 'layouts/messages') %>");